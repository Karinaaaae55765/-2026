# Q2 Method Card（条件接收修正版）

## 目标与题意解释

已知第一检测点 \(S_1\) 已成功获得示向度，先给出所有可保证第二次仍有响应的检测点区域，再在其中选择鲁棒定位效果最好的 \(S_2\)。题目未提供具体 \(S_1\) 与首测角度，因此代码输出的是可复用策略；默认坐标仅用于展示算法运行结果。

第二次返回 `near` 并非定位失败：它表示检测点距干扰源不超过 5 m，可以直接目视定位并清除。只有要求“第二次一定再得到示向度”时，才需从响应候选域进一步删去可能返回 `near` 的点。

## 物理信息与不确定集

记干扰源为 \(G\)，其固定有效接收半径为 \(R\in[1000,1500]\) m。第一次成功且未返回 `near` 给出联合不确定集

\[
\mathcal U_1=\{(G,R):G\in\Omega_1,\ 1000\le R\le1500,\ 5<\|G-S_1\|\le R\},
\]

其中

\[
\Omega_1=W_1\cap B(O,1800)\cap B(S_1,1500)\setminus B(S_1,5).
\]

对给定的可能目标 \(G\)，与首测信息相容的最小接收半径不是恒为 1000 m，而是

\[
R_{\min}(G)=\max\{1000,\|G-S_1\|\}.
\]

## 第二检测点候选区域

保证第二点获得响应的候选域为

\[
\mathcal C_{\rm sig}=\left\{S_2\ne S_1:\|S_2-G\|\le R_{\min}(G),\ \forall G\in\Omega_1\right\}.
\]

保证第二点一定返回示向度而不会返回 `near` 的子区域为

\[
\mathcal C_{\rm bearing}=\left\{S_2\in\mathcal C_{\rm sig}:\|S_2-G\|>5,\ \forall G\in\Omega_1\right\}.
\]

因此应分别报告两个区域：\(\mathcal C_{\rm sig}\) 是完成题目定位/清除任务所需的响应候选域；\(\mathcal C_{\rm bearing}\) 是纯双示向度方案的候选域。代码用有限目标样本和网格近似这两个连续区域，不能把网格边界写成解析精确边界。

## 主方法与基线

| ID | 角色 | 方法 | 主要局限 |
|---|---|---|---|
| M2-RR | 主方法 | 在 \(\mathcal C_{\rm sig}\) 上最小化最坏场景定位区域的最小包围圆半径 | 内外层均为确定性有限离散，只是连续问题近似 |
| M2-ANGLE | 可用基线 | 在同一 \(\mathcal C_{\rm sig}\) 上最大化最坏交会角正弦 | 交会角只是定位误差的代理指标 |
| M2-SLACK | 条件后备 | 当条件响应候选域为空时，最小化接收约束违约量 | 不再保证所有相容场景都有第二次响应 |

对于场景 \(G\in\Omega_1\)，若 \(\|S_2-G\|\le5\)，第二次直接近距定位，定义损失为 0。否则第二示向角为

\[
\alpha_2=\operatorname{atan2}(G_y-S_{2y},G_x-S_{2x})+e_2,\quad e_2\in[-1^\circ,1^\circ],
\]

并保留首测后的全部信息：

\[
P_2(S_2;G,e_2)=\Omega_1\cap W_2(S_2,\alpha_2)\cap B(S_2,1500).
\]

其中核心定义是新版 \(P_2=\Omega_1\cap W_2\)，最后一个圆盘显式保留第二次成功接收所给出的 1500 m 上界。旧版 \(W_1\cap W_2\) 不得再使用。

令 \(\rho(P)\) 为集合 \(P\) 的最小包围圆半径，则

\[
L(S_2;G,e_2)=\begin{cases}0,&\|S_2-G\|\le5,\\ \rho(P_2(S_2;G,e_2)),&\|S_2-G\|>5,\end{cases}
\]

\[
J(S_2)=\max_{G,e_2}L(S_2;G,e_2),\qquad S_2^*=\arg\min_{S_2\in\mathcal C_{\rm sig}}J(S_2).
\]

数值近似同优时，以移动距离 \(\|S_2-S_1\|\) 较小者破同分；但 \(S_2=S_1\) 必须排除，因为同址重复测量不能形成新的几何交会信息。

## 求解与验证流程

1. 从首测示向楔形、场地圆域、最大接收距离和 `near` 条件构造 \(\Omega_1\)。
2. 对候选平面网格逐点检查条件半径 \(R_{\min}(G)\)，分别输出响应候选域和示向候选域的完整点表。
3. 在响应候选域中用全局粗网格、结构化种子和局部加密搜索第二点。
4. 遍历目标样本和包含 \(\pm1^\circ\) 端点的误差样本；近距场景记零损失，其余显式裁剪 \(P_2\)。
5. 用最小包围圆半径形成最坏情形目标，并用交会角基线在相同候选域上作公平比较。
6. 检查条件半径相较错误固定 1000 m 规则所恢复的合法点、同址排除、近距分支、跨零角、误差端点、旧定义无界回归和网格敏感性。

## 主张边界

- 可主张：算法正确利用了首测成功这一条件信息，且区分“保证响应”和“保证再次示向”两种候选区域。
- 可主张：在保存的有限网格和场景集上，主方法取得所报告的最坏半径。
- 不可主张：默认合成坐标是题目唯一的第二检测点；网格面积是解析面积；有限离散结果证明了连续全局最优。

## Compact history

- 2026-09-10：确定以鲁棒最小包围圆半径评价第二检测点。
- 2026-09-11：废止固定 1000 m 候选约束，改为由首次成功检测条件导出的 \(R_{\min}(G)\)；区分响应域与纯示向域，并把 `near` 改为直接成功分支（decision_id: `q2_conditional_reception_2026-09-11`）。

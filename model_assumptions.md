# Q1—Q2 建模假设与代码交接稿（当前唯一对外版本）

## 目标

根据多个检测点坐标和同一干扰源在各检测点的示向度，构造交会定位的凸多边形区域，采用旋转卡壳法计算区域直径，并判定以该直径端点对为直径的圆是否覆盖整个定位区域。

## 一、题面事实（必须遵守）

| ID | 陈述 | 来源与建模用途 | 违背时的影响 |
|---|---|---|---|
| F1 | 所有对象在同一二维平面，坐标单位为米；东为 x 正向、北为 y 正向。 | 题目附录2。确定坐标与距离计算。 | 不能直接使用二维欧氏距离。 |
| F2 | 方位角从 x 轴正向逆时针计，范围为 [0°,360°)。 | 题目附录2。确定示向度的方向。 | 扇形方向颠倒或旋转。 |
| F3 | 在检测点处，示向度与真实方位角的误差落在 [-1°,1°]。 | 题目附录2。每次观测给出一个 2° 的可能方向扇形。 | 不能把示向度作为精确射线。 |
| F4 | 同一地点重复检测不会改变该地误差。 | 题目附录2。Q1 输入为各点的一次示向度，不把重复读数当独立样本。 | 不应擅自平均同地点读数降低误差。 |
| F5 | 干扰源位置在定位过程中不变化。 | 干扰源频道不随时间变化及 Q1 静态交会定位语境。 | 多点示向无法指向同一静态位置。 |

## 二、Q1 采用的模型约定

| ID | 约定 | 原因 | 影响与处理 |
|---|---|---|---|
| M1 | 误差按确定性有界误差处理，不假设正态、独立或同分布。 | 题目只给出误差范围。 | 输出是可能位置区域，不是概率置信椭圆。 |
| M2 | 第 i 次测向的可行区域是以检测点 S_i 为顶点、方位区间 [alpha_i-1°, alpha_i+1°] 的闭合无限楔形 W_i（无圆弧半径边界）。 | 将题设误差直接转为集合约束；与有界方位误差的集合估计模型一致。 | 误差端点也应纳入可行区域；不得误画成半径有限的扇形。 |
| M3 | 主定位区域定义为 P=交集(W_i)，不默认与半径 1800m 的目标圆域相交。 | Q1 明确称其为“多边形定位区域”；裁入圆域会出现圆弧，改变问题对象。 | 可另提供 `use_arena_clip` 扩展，但不得与本题主算法混用。 |
| M4 | 扇形顶点 S_i 在计算中作为闭集边界保留。 | 便于半平面求交与数值稳定；真实示向在距离为0时没有意义。 | 只会影响极端退化情形，不影响正常区域直径。 |
| M5 | 有效接收半径、频道、移动和清除规则均不进入 Q1。 | 它们属于 Q3/Q4 的在线搜索条件，Q1 的输入已经给定有效示向度。 | 不要向 Q1 代码加入模拟器调用或路径代价。 |

## 三、输入与输出

### 输入

```text
observations: 长度 n 的列表，n >= 1
  observations[i].x, observations[i].y: 检测点坐标（float，单位 m）
  observations[i].bearing_deg: 示向度（float，单位 °，允许任意实数，程序内归一化）
delta_deg: 默认 1.0
tol: 可选；默认采用与坐标尺度相关的几何容差
```

### 输出

```text
status:
  OK             有界、非退化凸定位多边形
  DEGENERATE     单点或线段定位区域
  EMPTY          全部扇形没有公共点
  UNBOUNDED      公共区域存在无穷远方向，区域直径无定义

vertices_ccw: 逆时针顶点列表，去重后不重复首尾点
diameter: D（m）；仅 OK / DEGENERATE 有定义
diameter_pair: A, B；满足 |A-B|=D 的一对顶点
diameter_circle:
  center: (A+B)/2
  radius: D/2
  covers_region: bool
  max_vertex_distance: max_j |V_j-center|
diagnostics:
  num_observations, num_candidate_intersections, tolerance, optional violations
```

## 四、核心几何定义

将所有角度换算为弧度。对第 i 个观测：

```text
theta_minus = radians(normalize(alpha_i - delta_deg))
theta_plus  = radians(normalize(alpha_i + delta_deg))
u_minus = (cos(theta_minus), sin(theta_minus))
u_plus  = (cos(theta_plus),  sin(theta_plus))
```

二维叉积定义为：

```text
cross((a_x,a_y), (b_x,b_y)) = a_x*b_y - a_y*b_x
```

令 q=G-S_i。点 G 位于第 i 个误差扇形内，当且仅当：

```text
cross(u_minus, q) >= -tol
cross(q, u_plus)  >= -tol
```

注意：不要用斜率表示方向；它会在 90° 附近失效，也容易在 0°/360° 处产生断裂。以 sin/cos 方向向量和叉积实现可以自然处理角度环绕。

## 五、定位多边形算法

1. 对每个观测产生两条误差边界直线 L_i^-、L_i^+；方向分别为 u_minus、u_plus，且均过 S_i。
2. 枚举任意两条不平行边界直线，计算其交点 p。
3. 若 p 满足全部观测的两条叉积不等式，则保留 p。
4. 对保留点进行容差去重；取其凸包，并输出为逆时针顶点序列。
5. 先判定公共方向是否存在：若存在单位向量 d 同时满足每个观测的两条“齐次”叉积不等式，则 P 无界，返回 UNBOUNDED，不计算直径。
   - 实现提示：枚举所有 theta_minus、theta_plus 对应的单位方向 d，并逐个检查全部齐次不等式；若公共方向非空，其端点必来自某条边界方向。
6. 若不存在公共方向且没有可行交点，返回 EMPTY；若凸包仅有1或2个顶点，返回 DEGENERATE。

两条直线可写为：

```text
p = s_a + t*u_a
q = s_b + r*u_b
t = cross(s_b-s_a, u_b) / cross(u_a, u_b)
```

当 `abs(cross(u_a,u_b)) <= tol_parallel` 时，将其视作平行或近似平行，不求交。

## 六、直径：旋转卡壳法（正式算法）

前置条件：`vertices_ccw` 是已去重的凸包，且顶点按逆时针排列。

若 m=1，则 D=0；若 m=2，则 D 为两点距离。若 m>=3，采用旋转卡壳：

```text
j = 1
for i = 0,...,m-1:
    ni = (i+1) mod m
    while area(V_i, V_ni, V_(j+1 mod m))
          > area(V_i, V_ni, V_j) + tol_area:
        j = (j+1) mod m
    用 (V_i,V_j) 与 (V_ni,V_j) 更新当前最大距离
    若两面积在 tol_area 内相等，也检查 V_(j+1 mod m)
```

其中：

```text
area(A,B,C) = abs(cross(B-A, C-A))
```

记录最大距离对应的端点 A、B。旋转卡壳是凸多边形最远点对的经典精确算法，时间复杂度 O(m)；不得把它用于尚未排序、非凸或含重复顶点的点集。

开发阶段另以 O(m^2) 的顶点对枚举计算一个 `diameter_bruteforce`，并断言它与旋转卡壳结果在容差内相同；该枚举仅作测试，不作为论文主算法。

## 七、直径圆覆盖判定

对旋转卡壳得到的直径端点 A、B：

```text
C = (A+B)/2
R = distance(A,B)/2
max_d = max(distance(V_j,C) for each hull vertex V_j)
covers_region = (max_d <= R + tol)
```

此判定是充分且必要的：圆若以半径 D/2 覆盖距离为 D 的端点 A、B，其圆心只能是二者中点 C；凸多边形内任意点到固定圆心的最大距离可在顶点处取得。因此，无需额外检查边内点。

## 八、数值与异常处理

- 使用双精度浮点数。
- 设坐标尺度 L 为输入坐标绝对值、候选交点绝对值和1中的最大值；可取 `tol = 1e-9 * L`，`tol_parallel = 1e-12`。所有比较统一采用该容差。
- 统一将 bearing 归一化到 [0°,360°)，但不要用区间端点的大小关系判断扇形；方向向量方式天然支持 359° 到 1° 的跨零情形。
- 容差去重后再求凸包；去掉共线中间点，保留线段两端点。
- 不将 EMPTY、UNBOUNDED、DEGENERATE 伪装为普通多边形结果；调用端必须读取 status。

## 九、最小验收测试

1. **一致性**：任选真实点 G 和多个检测点，令 alpha_i 为 S_i 指向 G 的方位角；构造的 P 应包含 G。
2. **误差边界**：对每个 alpha_i 施加不超过 ±1° 的扰动，真实 G 仍应通过全部扇形约束。
3. **跨零角度**：使用 alpha=0° 或359.8° 的观测，确认扇形方向正确。
4. **直径核验**：随机生成凸多边形，将旋转卡壳直径与 O(m^2) 枚举结果比较。
5. **覆盖反例**：对近似等边三角形，验证 `max_vertex_distance > D/2`，返回不覆盖。
6. **异常**：构造共同方向存在的测向集合，返回 UNBOUNDED；构造不相容扇形，返回 EMPTY。
7. **可视化**：绘制检测点、两条误差边界、定位多边形、直径端点和直径圆，人工核对一组测试图。

## 十、代码边界

- 本模块只完成 Q1 的几何定位区域与评价，不访问模拟器，不调用 HTTP 接口。
- 不加入频道、接收半径、移动耗时、清除半径、定向源等 Q3/Q4 内容。
- 不使用随机模拟、拟合、机器学习或优化器代替题设的确定性几何约束。

# Q2 鲁棒第二检测点模型假设

## 题面事实

| ID | 陈述 | 建模用途 | 违反时影响 |
|---|---|---|---|
| Q2-F1 | 干扰源为全向源。 | 只要距离不超过有效接收半径就能接收，不存在定向覆盖盲区。 | 候选区域的鲁棒接收约束不再成立。 |
| Q2-F2 | 有效接收半径位于1000至1500m且具体值未知。 | 用1000m构造保证接收的稳健约束；首测成功可推出目标距首测点不超过1500m。 | 若范围变化，应同步修改候选区域。 |
| Q2-F3 | 首测已经得到示向度。 | 目标距首测点大于5m，且位于首测误差扇形中。 | 若实际返回near/no_signal，则本模型入口条件不成立。 |
| Q2-F4 | 同地点误差固定。 | 第二点必须与首测点不同，原地复测不增加方向信息。 | 重复采样平均不能缩小误差。 |

## 方法约定

| ID | 陈述 | 建模需要 | 验证与风险 |
|---|---|---|---|
| Q2-M1 | 首测后的不确定集合为 Ω1=W1∩B(O,1800)∩B(S1,1500)\B(S1,5)。 | 汇总题面可用的全部位置知识；圆域只表示物理可行性，不代表圆内均匀分布。 | 对跨0°扇形、圆域边界和5m开边界进行数值测试。 |
| Q2-M2 | 候选点以最小接收半径1000m进行鲁棒设计。 | 不知道实际接收半径时保证第二次能接收。 | 可能偏保守；若候选域为空，启动带接收违约量的后备模型。 |
| Q2-M3 | 以最小包围圆半径作为定位误差尺度。 | 它表示选择最优圆心后可能发生的最大位置误差，并与20m清除半径直接可比。 | 同时报告区域直径，检查半径指标是否掩盖细长形状。 |
| Q2-M4 | 不给示向误差指定概率分布，而对真实位置与第二次误差取最坏情况。 | 题目只提供误差界。 | 鲁棒结果可能保守；用典型场景和交会角基线辅助解释。 |
| Q2-M5 | 移动距离不进入首要目标，只在鲁棒半径近似相同时作为次级准则。 | Q2要求“定位效果”，未明确要求时间最短。 | Q3复用时再将移动时间纳入联合决策。 |
| Q2-M6 | 第二次获得示向度后，完整可行定位区域定义为 P2=Ω1∩W2，而非仅 W1∩W2。 | 首测已知的目标圆域、1500m距离上界和非near信息不能在第二次更新时丢失。 | P2含圆弧边界；代码可用保守正多边形逼近圆，并做分辨率收敛检查。 |
| Q2-M7 | 两次观测只负责取得“尽可能好的”定位结果，不假设必然达到20m清除精度。 | 两个有界误差楔形在不利几何下仍可能形成较大区域。 | 若最优最坏包围圆半径仍大于20m，Q2如实报告；Q3继续增加第三测点。 |
| Q2-M8 | 长轴法向的两个对称点只作为候选生成和局部搜索初值，不代替鲁棒目标。 | ICRA 2012 的方向选择思想可降低搜索量，但其高斯/EKF假设不适用于本题。 | 最终点仍由最坏最小包围圆半径决定；效果近似时选移动更短者。 |
| Q2-M9 | 不假设干扰源在半径1800m圆内均匀分布，也不因靠近圆周而人为降低出现概率。 | 题面没有提供空间概率先验，Q2采用最坏情况保证。 | 圆周区域仍须纳入候选点评价；若未来有训练数据，再单独建立概率型对照模型。 |

## Q2 完整计算口径

设第一检测点为 S1，首测示向度为 alpha1，误差上界 delta=1°。第一楔形记为 W1，首测后物理可行集合为

```text
Omega1 = W1 ∩ arena_disk ∩ disk(S1,1500) \ disk(S1,5).
```

严格保证第二次返回示向度的候选区域为

```text
C = {S2:
     max_{G in Omega1} distance(S2,G) <= 1000,
     min_{G in Omega1} distance(S2,G) > 5}.
```

其中1000m来自未知有效接收半径的最小值；大于5m用于避免第二次返回near而没有示向角。如果研究目的允许“near后直接清除”也算优于定位，则可另外保留near候选分支，但不能与取得第二条示向度的主模型混写。

对任一候选S2、任一可能真实位置G∈Omega1和第二点误差e2∈[-1°,1°]：

```text
true_bearing_2 = atan2(G_y-S2_y, G_x-S2_x)
measured_bearing_2 = true_bearing_2 + e2
W2 = wedge(S2, measured_bearing_2, delta=1°)
P2 = Omega1 ∩ W2
rho(P2) = P2的最小包围圆半径
```

候选点的最坏损失及最优点为

```text
J(S2) = max_{G in Omega1, e2 in [-1°,1°]} rho(P2)
S2_star = argmin_{S2 in C} J(S2).
```

若多个候选点的J差异不超过数值容差，选择从S1（Q3复用时从机器狗当前位置）移动距离较短者。输出至少包括：候选区域C、S2_star、J(S2_star)、对应最坏场景、是否满足J<=20m。

## Q2 候选生成与求解步骤

1. 将圆域及B(S1,1500)用外接/内接高分辨率正多边形表示，并构造Omega1；改变圆离散边数，检查最优点和J是否收敛。
2. 计算Omega1的最小面积外接矩形，取其最长轴u及法向n；也可用边界点PCA作近似，但最终以几何目标复核。
3. 以Omega1代表中心c构造两族种子点S2_plus(r)=c+r*n、S2_minus(r)=c-r*n，并与候选区域C求交。
4. 同时在C上保留低密度全局网格，防止长轴启发式漏掉更优区域。
5. 对全部粗候选遍历G与e2场景，计算P2及其最小包围圆半径，得到J。
6. 在当前最优点附近局部加密；同步加密G、e2和圆边界离散，直到点位与目标值稳定。
7. 用“最大化最坏交会角”的基线点代入同一个J函数比较，证明鲁棒模型是否真正改善最坏定位半径。
8. 若C为空，明确报告严格保证接收的第二点不存在；后备模型可最小化1000m接收约束的最大违约量，但必须标注不再具有必然收到信号的保证。

## 文献方法带来的边界说明

- ICRA 2013 的有界方位误差楔形交集与最坏直径模型，为Q1/Q2提供理论依据；三角布设说明第三测点从缺失方向包围目标通常更稳健。
- ICRA 2012 的“沿最大不确定方向的法向选点、两个对称点择近”只用于加速候选搜索和减少移动。
- 不采用ICRA 2012的高斯噪声、EKF协方差或同地点重复测量结论；这些条件与题面“同地点误差不变”冲突。
